/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com', 
      'source.unsplash.com', 
      'placehold.co',
      'videos.openai.com'
    ],
  },
};

export default nextConfig;
